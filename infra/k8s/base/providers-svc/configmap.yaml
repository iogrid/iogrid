---
# providers-svc runtime config (non-secret). Referenced via envFrom in the
# Deployment. Captured from the live prod Deployment (2026-06-03, #640).
apiVersion: v1
kind: ConfigMap
metadata:
  name: providers-svc-config
  namespace: iogrid
  labels:
    app.kubernetes.io/name: providers-svc
    app.kubernetes.io/part-of: iogrid
    app.kubernetes.io/component: providers
data:
  GEOIP_DB_PATH: "/var/lib/iogrid/geoip/dbip-city-lite.mmdb"
