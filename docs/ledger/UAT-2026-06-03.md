# iogrid — Pre-Go-Live UAT Results (2026-06-03)

> 🗄️ Historical — point-in-time UAT snapshot captured 2026-06-03; some rows
> were superseded later the same day. Notably **#642** advanced past the
> `pg_dumpall` stop-gap shown below to a full in-cluster **MinIO + continuous
> WAL archiving / PITR** fix (ContinuousArchiving=True, base backup completed).
> For current state always defer to [`TRACKER.md`](./TRACKER.md) + [`TRUST.md`](./TRUST.md).

**Method:** 8 parallel UAT agents + live browser walk, end-user perspective. Every gap has a ticket.

## 🔁 Round 2 — aggressive remediation + INDEPENDENT acceptance (each fix re-verified by a separate agent before close)

| Issue | Fix | Independent verdict | State |
|---|---|---|---|
| **#648** OAuth redirect host | live env patched to apex + in-repo configmap | first **REJECT** (in-repo still had app.iogrid.org in ALLOWED_RETURN_HOSTS) → fixed → **RE-ACCEPT** | ✅ **CLOSED** |
| **#639** vpn-svc NetworkPolicy | restored targeted allow-policy, applied live w/ canary + auto-rollback, back in repo | **ACCEPT** (adversarial — zero breakage, egress allowlist complete) | ✅ **CLOSED** |
| **#629** mobile (4 gaps) | Apple sign-in wired (was a stub), ping-pay test fixed, jest-in-CI, AASA content-type + team-ID build-arg | **ACCEPT all 4** | ✅ mobile done (external C-8/mint remain) |
| **#646** Google sign-in | wiring verified turnkey | **ACCEPT-AS-BLOCKED** | ⛔ operator (Google creds) |

The independent round **caught a real miss** (#648 in-repo drift that would have re-broken on a GitOps apply) — exactly its purpose.

## ⚡ Remediation (same session) — 3 of 5 P1 blockers FIXED + LIVE

| Blocker | Outcome |
|---|---|
| **#643 Install flow** | ✅ **FIXED + LIVE** — download URLs → working `/latest/` CDN (7/7 verified 200); `/install/{sh,mac,win,linux}` now serve the daemon installer. Downgraded p1→p3 (only the status.iogrid.org footer link remains). |
| **#644 Missing proxy routes** | ✅ **FIXED + LIVE + CLOSED** — persona-picker / VPN-sessions / staking routes now 401/405 (routed). |
| **#642 DB backups** | ✅ **STOP-GAP LIVE** — `pg_dumpall` CronJob (6-hourly, superuser, local PVC) verified producing a 627K full backup. Zero-backups risk resolved. WAL/PITR still needs Hetzner creds (operator). |
| #649 Flaky builds | ✅ FIXED — Inter vendored, zero build-time font CDN calls. |
| #648 OAuth redirect | ✅ FIXED — → apex (prereq for #646). |
| #629 mobile tests | ✅ FIXED — ping-pay 24/24, jest now gates every build. |
| **#646 Google sign-in** | ⛔ **operator** — real Google OAuth creds. |
| #629 mobile (Apple/AASA) | 🟡 Apple-stub product call + team-ID + AASA content-type. |

**Remaining hard blockers: #646 (Google OAuth creds) + #642-WAL (Hetzner creds) — both need operator credentials.**

---

## Original VERDICT (pre-remediation): 🔴 NOT GO-LIVE READY — 5 P1 blockers

The product's core *paid/authenticated* journeys and onboarding have hard blockers. Marketing, browsing, and the provider/customer/VPN dashboards themselves are solid.

---

## Business-capability scorecard

> **CURRENT STATE (post-remediation, refreshed 2026-06-03).** The `Initial` column is what the UAT first found; `Now` is the live state after remediation + independent acceptance. Original verdict was 🔴 5 P1 blockers; **all 5 P1s are now resolved or down to an operator credential.**

| # | Business capability (what a user does) | Initial | Now | Ticket |
|---|---|---|---|---|
| 1 | Visit site, browse products, read pricing/legal | ✅ | ✅ PASS | — |
| 2 | **Provider: download + install the daemon** | 🔴 FAIL | ✅ **FIXED+LIVE** — `/install/{sh,mac,win,linux}` + download buttons all work | #643 ✅closed |
| 3 | Provider: dashboard, schedule, earnings, staking, payout | ✅ | ✅ PASS | — |
| 4 | Customer: pricing → buy → billing (prepaid+grace), API keys, usage | ✅ | ✅ PASS | — |
| 5 | Consumer VPN: pricing, upgrade (Stripe), CLI install | ✅ | ✅ PASS | — |
| 6 | Sign in with **email magic-link** (web) | ✅ | ✅ PASS | — |
| 7 | Sign in with **Google** (web) | 🔴 FAIL | 🟡 broken button now **HIDDEN** (graceful); enabling Google needs operator OAuth creds | #646 (operator) |
| 8 | Sign in with **Apple** (mobile) | 🔴 FAIL | ✅ **FIXED** — real `signInWithApple()` wired (was a stub) | #629 |
| 9 | Account: notifications, sessions, wallets | ✅ | ✅ PASS | #641 ✅closed |
| 10 | Admin console (separate app) | ✅ | ✅ PASS | — |
| 11 | $GRID token page | ✅ | ✅ PASS | — |
| 12 | **Mobile app (TestFlight build 137)** | 🟡 | ✅ **PASS** — Apple sign-in + Universal Links now live | #629 |
| 13 | **Ping $GRID payments (Universal Links)** | 🟡 | ✅ **FIXED** — AASA `application/json` + real team ID live; devnet $GRID mint deployed | #629 (Ping C-8 external) |
| 14 | API / coordinator (11 microservices) | ✅ | ✅ PASS | — |
| 15 | Web↔API proxy completeness | 🟡 | ✅ **FIXED+LIVE** — persona-picker / vpn-sessions / staking routes | #644 ✅closed |
| 16 | **Database durability (backups / PITR)** | 🔴 FAIL | ✅ **FIXED** — continuous WAL archiving + PITR to in-cluster MinIO (independently accepted) | #642 ✅closed |
| 17 | TLS / HTTPS certificates | 🟡 | ✅ **FIXED** — renewal pipeline repaired (http-01) | #645 ✅closed |
| 18 | Platform / deploy (cluster, images, edge) | ✅ | ✅ PASS; off-prod runtime-validation gate now GREEN in CI | #637 |
| 19 | Network security (vpn-svc policy) | 🟡 | ✅ **FIXED** — vpn-svc-allow restored (independently accepted) | #639 ✅closed |
| 20 | Status page link | 🔴 FAIL | ✅ footer → `/status` apex (200) | #643 ✅closed |

**Score now: 17 ✅ PASS · 2 🟡 (Google feature = operator creds; Ping C-8 = external) · 0 🔴 P1 FAIL** — up from 11 PASS / 5 PARTIAL / 5 FAIL.

### Remaining before go-live (all credential / external / founder decisions)
- **#646** — paste a real Google OAuth client → web Google sign-in works (button auto-reappears).
- **#629** — Ping's C-8 sig-verify spec (their side) + your mainnet `$GRID` mint call (devnet proven).
- **#637** — your authorization to unsuspend the Flux Kustomization (validation green, reconcile is a clean no-op).
- **#574** — your ASC session cookie for the privacy-nutrition-labels automation (external-beta already in Apple review).
- **#652** — Hetzner creds for *offsite* DR (in-cluster PITR works now).

---

## 🔴 Go-live blockers (must fix before launch)

| Ticket | Blocker | Who unblocks |
|---|---|---|
| **#643** | Install/download flow 404 — a provider literally cannot install the daemon | engineering (wrong URL template + missing `/install/*` routes) |
| **#646** | Google sign-in broken — `GOOGLE_CLIENT_ID` is `phase0-placeholder` | **operator** (provision real Google OAuth creds) |
| **#642** | Production DB has **no backups at all** — unrecoverable on failure | **operator** (Hetzner S3 creds) |
| **#629** | Mobile: Apple sign-in button is a stub + Universal Links AASA broken | engineering |
| **#644** | 3 web pages broke on missing API proxy routes (persona-picker, VPN sessions, staking) — **fixed, needs deploy** | engineering (deploy) |

## 🟡 Should-fix soon (not launch-blocking)
- **#645** TLS renewal will fail ~Aug 4 (apex + proxy) → future outage.
- **#648** identity-svc Google redirect URL points at the dropped `app.iogrid.org`.
- **#629** ping-pay unit test failing + **CI runs zero jest** (mobile code untested) + stale 6-decimal comment.
- **#639** vpn-svc has no NetworkPolicy (defense-in-depth).
- Admin app missing favicon (cosmetic).

## ✅ Verified solid
Marketing site (0 console errors across homepage/token/pricing/sign-in), provider dashboard (#630/#633/#634 fixes confirmed), customer billing incl. prepaid+grace model (#632/#635), consumer VPN, email magic-link sign-in, account surfaces (#641 fix confirmed 404→401), admin separation, the 9 coordinator microservices, deploy pipeline (0 drift), TestFlight delivery (build 137 VALID on external beta).

---
*Generated by the autonomous UAT campaign, 2026-06-03. Closed/verified this session: #575, #638, #641. Tickets opened by UAT: #642, #643, #644, #645, #646, #648 + #629 gaps.*
