packages:
  - "admin"
  - "web"
  # marketing/ workspace was folded into web/ in EPIC #422 Phase 3.
  # The product app now serves the apex (iogrid.org) and carries the
  # public landing + about + pricing + legal + status pages.
  - "docs-site"
  - "sdks/typescript"

# Note: workspace-wide pnpm overrides live in the root /package.json
# under `pnpm.overrides` (see PR #160 for rationale). Repeating them
# here would conflict with the package.json copy — pnpm honours one
# location only.
